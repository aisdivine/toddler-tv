name: Deploy preview to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build # → dist/counting (the live preview, not the mp4)
      - name: Write the toddler-tv landing page
        run: |
          cat > dist/index.html <<'EOF'
          <!doctype html>
          <html lang="en"><head><meta charset="utf-8">
          <meta name="viewport" content="width=device-width, initial-scale=1">
          <title>Toddler TV</title>
          <style>
            body{margin:0;min-height:100vh;display:grid;place-items:center;
              font-family:system-ui,sans-serif;background:#0e1530;color:#eaf0ff}
            .card{text-align:center;padding:2rem}
            a{display:inline-block;margin-top:1rem;padding:.8rem 1.4rem;border-radius:.6rem;
              background:#ffd23d;color:#0e1530;text-decoration:none;font-weight:700}
            h1{font-weight:800;letter-spacing:.02em}
          </style></head>
          <body><div class="card">
            <h1>📺 Toddler TV</h1>
            <p>Math-made videos for little ones.</p>
            <a href="./counting/">▶ Let's Count! (live preview)</a>
          </div></body></html>
          EOF
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
